name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:19.03.12-dind
        options: --privileged

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build and push frontend with cache
        run: |
          IMAGE_NAME=smellinspector-companion-frontend
          REPO=ghcr.io/${{ github.repository_owner }}/$IMAGE_NAME
          SHORT_SHA=$(echo "${GITHUB_SHA}" | cut -c1-7)

          docker buildx build \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            --cache-from=type=local,src=/tmp/.buildx-cache \
            --cache-to=type=local,dest=/tmp/.buildx-cache-new \
            --file frontend/Dockerfile \
            --tag "$REPO:latest" \
            --tag "$REPO:$SHORT_SHA" \
            $( [ "$GITHUB_REF_NAME" = "develop" ] && echo --tag "$REPO:dev" ) \
            --push frontend

      - name: Build and push ml-backend with cache
        run: |
          IMAGE_NAME=smellinspector-companion-ml-backend
          REPO=ghcr.io/${{ github.repository_owner }}/$IMAGE_NAME
          SHORT_SHA=$(echo "${GITHUB_SHA}" | cut -c1-7)

          docker buildx build \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            --cache-from=type=local,src=/tmp/.buildx-cache \
            --cache-to=type=local,dest=/tmp/.buildx-cache-new \
            --file machine_learning/Dockerfile \
            --tag "$REPO:latest" \
            --tag "$REPO:$SHORT_SHA" \
            $( [ "$GITHUB_REF_NAME" = "develop" ] && echo --tag "$REPO:dev" ) \
            --push machine_learning

      - name: Move Docker cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
